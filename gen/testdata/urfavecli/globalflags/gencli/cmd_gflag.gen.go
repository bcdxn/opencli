// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
package gencli

import (
	"context"
	"github.com/bcdxn/opencli/spec"
	"github.com/urfave/cli/v3"
)

func NewCmdGflag(a ActionsInterface) *cli.Command {
	cmd := &cli.Command{
		Name:        "gflag",
		Usage:       "A minimal spec that declares real (non-help/version) global flags to exercise the context/accessor codegen paths for every framework.",
		Description: "`gflag` is a tiny fixture whose only purpose is to declare genuine global flags so\ngenerated output exercises the `GlobalFlagsFromContext` / `setGlobalFlags` /\n`getGlobalFlags` helpers and their call sites in each command handler.\n\nIt also declares one leaf per return-branch shape (args+flags, args-only,\nflags-only, neither) so all four action-call forms appear in the goldens.\n",
		Metadata:    map[string]any{"spec_cmd": getSpecGflagCmd()},
		Action: func(ctx context.Context, c *cli.Command) error {
			return BadUserInput("subcommand is required", func() error {
				return a.UsageFunc(getSpecGflagCmd())
			})
		},
	}
	cmd.Commands = append(cmd.Commands, NewCmdGflagPing(a))
	cmd.Commands = append(cmd.Commands, NewCmdGflagEcho(a))
	cmd.Commands = append(cmd.Commands, NewCmdGflagGreet(a))
	cmd.Commands = append(cmd.Commands, NewCmdGflagSend(a))

	return cmd
}

func getSpecGflagCmd() *spec.CommandItem {
	return &spec.CommandItem{
		Segment:         "gflag",
		CommandLine:     "gflag",
		Summary:         "A minimal spec that declares real (non-help/version) global flags to exercise the context/accessor codegen paths for every framework.",
		Description:     "`gflag` is a tiny fixture whose only purpose is to declare genuine global flags so\ngenerated output exercises the `GlobalFlagsFromContext` / `setGlobalFlags` /\n`getGlobalFlags` helpers and their call sites in each command handler.\n\nIt also declares one leaf per return-branch shape (args+flags, args-only,\nflags-only, neither) so all four action-call forms appear in the goldens.\n",
		VisibleChildren: true,
		VisibleArgs:     false,
		VisibleFlags:    false,
		CommandModifiers: []string{
			"{command}",
		},
		ArgsModifiers: []string{
			"<arguments>",
		},
		FlagsModifiers: []string{
			"[flags]",
		},
		Commands: []*spec.CommandItem{
			{Segment: "ping", Summary: "Report that the CLI is alive"},
			{Segment: "echo", Summary: "Echo a single argument back"},
			{Segment: "greet", Summary: "Greet someone by name"},
			{Segment: "send", Summary: "Send a message the given number of times"},
		},
	}
}
