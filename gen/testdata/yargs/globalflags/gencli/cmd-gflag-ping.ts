// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
  GflagOutputFormat,

  GlobalFlags,
  setGlobalFlags,
} from "./params";
import {
  assertChoice,
  resolveStringFlag
} from "./config";

import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface gflagPingArgs {
  "debug"?: boolean;
  "timeout"?: number;
  "outputFormat": string | undefined;
  help: boolean;
}

export function newGflagPingCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, gflagPingArgs> {
  return {
    command: "ping",
    describe: "Report that the CLI is alive",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<gflagPingArgs> => {
      return argv
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getGflagPingCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getGflagPingCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getGflagPingCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<gflagPingArgs>) => {
      if (argv.help) {
        actions.help(getGflagPingCmdHelpData());
        process.exit(0);
      }
      setGlobalFlags({
        debug: argv.debug as boolean,
        timeout: argv.timeout as number,
        outputFormat: assertChoice("output-format", (resolveStringFlag(argv, ["outputFormat", "output-format"], [{ type: "$ENV", property: "GFLAG_OUTPUT_FORMAT" }, { type: "$FILE", property: "$.greeting.outputFormat" }])) as GflagOutputFormat | undefined, ["text", "json"]),
      });
      return actions.GflagPing();
    },
  };
}

function getGflagPingCmdHelpData(): CommandPrintData {
  return {
    segment: "ping",
    commandLine: "gflag ping",
    summary: "Report that the CLI is alive",
    visibleChildren: false,
    visibleArgs: false,
    visibleFlags: false,
  };
}
