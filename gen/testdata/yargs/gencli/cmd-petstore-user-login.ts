// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
  PetstoreUserLoginFlags,
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstoreUserLoginArgs {
  "username"?: string;
  "password"?: string;
  help: boolean;
}

export function newPetstoreUserLoginCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstoreUserLoginArgs> {
  return {
    command: "login",
    describe: "Log in a user",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstoreUserLoginArgs> => {
      return argv
        .option("username", {
          type: "string",
        })
        .option("password", {
          type: "string",
        })
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstoreUserLoginCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstoreUserLoginCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstoreUserLoginCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstoreUserLoginArgs>) => {
      if (argv.help) {
        actions.help(getPetstoreUserLoginCmdHelpData());
        process.exit(0);
      }
      const cmdFlags: PetstoreUserLoginFlags = {
        username: argv.username as string,
        password: argv.password as string,
      };
      return actions.PetstoreUserLogin(cmdFlags);
    },
  };
}

function getPetstoreUserLoginCmdHelpData(): CommandPrintData {
  return {
    segment: "login",
    commandLine: "petstore user login",
    summary: "Log in a user",
    visibleChildren: false,
    visibleArgs: false,
    visibleFlags: true,
    flagsModifiers: [
      "[flags]",
    ],
    flags: [
      { name: "username", summary: "The user's username" },
      { name: "password", summary: "The user's password" },
    ],
  };
}
