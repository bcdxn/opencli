// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
  PetstoreUserDeleteFlags,
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstoreUserDeleteArgs {
  "username": string;
  help: boolean;
}

export function newPetstoreUserDeleteCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstoreUserDeleteArgs> {
  return {
    command: "delete",
    describe: "Delete a user",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstoreUserDeleteArgs> => {
      return argv
        .option("username", {
          type: "string",
          demandOption: true,
        })
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstoreUserDeleteCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstoreUserDeleteCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstoreUserDeleteCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstoreUserDeleteArgs>) => {
      if (argv.help) {
        actions.help(getPetstoreUserDeleteCmdHelpData());
        process.exit(0);
      }
      const cmdFlags: PetstoreUserDeleteFlags = {
        username: argv.username as string,
      };
      return actions.PetstoreUserDelete(cmdFlags);
    },
  };
}

function getPetstoreUserDeleteCmdHelpData(): CommandPrintData {
  return {
    segment: "delete",
    commandLine: "petstore user delete",
    summary: "Delete a user",
    visibleChildren: false,
    visibleArgs: false,
    visibleFlags: true,
    flagsModifiers: [
      "[flags]",
    ],
    flags: [
      { name: "username", summary: "The username of the user to delete" },
    ],
  };
}
