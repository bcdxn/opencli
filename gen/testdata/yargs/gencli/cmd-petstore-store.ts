// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { newPetstoreStoreInventoryCmd } from "./cmd-petstore-store-inventory";
import { newPetstoreStoreOrderCmd } from "./cmd-petstore-store-order";
import { ActionsInterface } from "./actions";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
export function newPetstoreStoreCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}> {
  return {
    command: "store",
    describe: "A collection of commands for store operations",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<{}> => {
      return argv
        .command(newPetstoreStoreInventoryCmd(actions))
        .command(newPetstoreStoreOrderCmd(actions))
        .demandCommand(1)
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstoreStoreCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstoreStoreCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstoreStoreCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<{}>) => {
      if (argv.help) {
        actions.help(getPetstoreStoreCmdHelpData());
        process.exit(0);
      }
      return Promise.reject(
        createBadUserInputError("missing required sub command", () => {
          actions.usage(getPetstoreStoreCmdHelpData());
        }),
      );
    },
  };
}

function getPetstoreStoreCmdHelpData(): CommandPrintData {
  return {
    segment: "store",
    commandLine: "petstore store",
    summary: "A collection of commands for store operations",
    visibleChildren: true,
    visibleArgs: false,
    visibleFlags: false,
    commandModifiers: [
      "{command}",
    ],
    argsModifiers: [
      "<arguments>",
    ],
    flagsModifiers: [
      "[flags]",
    ],
    commands: [
      { segment: "inventory", commandLine: "inventory", summary: "Returns pet inventories by status" },
      { segment: "order", commandLine: "order", summary: "A collection of commands for purchase orders" },
    ],
  };
}
