// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { newPetstoreListCmd } from "./cmd-petstore-list";
import { newPetstorePetCmd } from "./cmd-petstore-pet";
import { newPetstoreStoreCmd } from "./cmd-petstore-store";
import { newPetstoreUserCmd } from "./cmd-petstore-user";
import { ActionsInterface } from "./actions";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
export function newPetstoreCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}> {
  return {
    command: "petstore",
    describe: "An example CLI Document describing operations a petstore CLI may provide.",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<{}> => {
      return argv
        .command(newPetstoreListCmd(actions))
        .command(newPetstorePetCmd(actions))
        .command(newPetstoreStoreCmd(actions))
        .command(newPetstoreUserCmd(actions))
        .demandCommand(1)
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstoreCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstoreCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstoreCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<{}>) => {
      if (argv.help) {
        actions.help(getPetstoreCmdHelpData());
        process.exit(0);
      }
      return Promise.reject(
        createBadUserInputError("missing required sub command", () => {
          actions.usage(getPetstoreCmdHelpData());
        }),
      );
    },
  };
}

function getPetstoreCmdHelpData(): CommandPrintData {
  return {
    segment: "petstore",
    commandLine: "petstore",
    summary: "An example CLI Document describing operations a petstore CLI may provide.",
    description: "`petstore` is an example command line interface designed to make working with the\n[PetStore API](https://petstore3.swagger.io) easier. It provides a number of\ncapabilities, including:\n\n- validating request parameters\n- executing API requests\n- parsing the API response\n- handling API errors\n\nThe commands are documented below. You can also find out more about each\ncommand using the contextual `--help` flag. e.g.:\n\n```sh\npetstore --help\n```\n",
    visibleChildren: true,
    visibleArgs: false,
    visibleFlags: false,
    commandModifiers: [
      "{command}",
    ],
    argsModifiers: [
      "<arguments>",
    ],
    flagsModifiers: [
      "[flags]",
    ],
    commands: [
      { segment: "list", commandLine: "list", summary: "List all endpoints available" },
      { segment: "pet", commandLine: "pet", summary: "A collection of commands for managing pets" },
      { segment: "store", commandLine: "store", summary: "A collection of commands for store operations" },
      { segment: "user", commandLine: "user", summary: "" },
    ],
  };
}
