// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
  PetstoreUserUpdateFlags,
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstoreUserUpdateArgs {
  "username": string;
  "firstName"?: string;
  "lastName"?: string;
  "email"?: string;
  "password"?: string;
  "phone"?: string;
  "userStatus"?: number;
  help: boolean;
}

export function newPetstoreUserUpdateCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstoreUserUpdateArgs> {
  return {
    command: "update",
    describe: "Update a user",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstoreUserUpdateArgs> => {
      return argv
        .option("username", {
          type: "string",
          demandOption: true,
        })
        .option("firstName", {
          type: "string",
        })
        .option("lastName", {
          type: "string",
        })
        .option("email", {
          type: "string",
        })
        .option("password", {
          type: "string",
        })
        .option("phone", {
          type: "string",
        })
        .option("userStatus", {
          type: "number",
          default: 0,
        })
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstoreUserUpdateCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstoreUserUpdateCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstoreUserUpdateCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstoreUserUpdateArgs>) => {
      if (argv.help) {
        actions.help(getPetstoreUserUpdateCmdHelpData());
        process.exit(0);
      }
      const cmdFlags: PetstoreUserUpdateFlags = {
        username: argv.username as string,
        firstName: argv.firstName as string,
        lastName: argv.lastName as string,
        email: argv.email as string,
        password: argv.password as string,
        phone: argv.phone as string,
        userStatus: argv.userStatus as number,
      };
      return actions.PetstoreUserUpdate(cmdFlags);
    },
  };
}

function getPetstoreUserUpdateCmdHelpData(): CommandPrintData {
  return {
    segment: "update",
    commandLine: "petstore user update",
    summary: "Update a user",
    description: "Update a user's information using a JSON payload or explicit flags.",
    visibleChildren: false,
    visibleArgs: false,
    visibleFlags: true,
    flagsModifiers: [
      "[flags]",
    ],
    flags: [
      { name: "username", summary: "The username of the user to update", aliases: [] },
      { name: "firstName", summary: "The user's first name", aliases: [] },
      { name: "lastName", summary: "The user's last name", aliases: [] },
      { name: "email", summary: "The user's email address", aliases: [] },
      { name: "password", summary: "The user's password", aliases: [] },
      { name: "phone", summary: "The user's phone number", aliases: [] },
      { name: "userStatus", summary: "The user's status", aliases: [] },
    ],
  };
}
