// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
  PetstoreStoreOrderPlaceArgs,
  PetstoreStoreOrderPlaceFlags,
  PetstoreStoreOrderPlaceStatus,
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstoreStoreOrderPlaceArgs {
  "path-to-order-body"?: string;
  "petId"?: number;
  "quantity"?: number;
  "status"?: string;
  "complete"?: boolean;
  help: boolean;
}

export function newPetstoreStoreOrderPlaceCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstoreStoreOrderPlaceArgs> {
  return {
    command: "place <path-to-order-body>",
    describe: "Place an order for a pet",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstoreStoreOrderPlaceArgs> => {
      return argv
        .positional("path-to-order-body", {
          describe: "",
          type: "string",
        })
        .option("pet-id", {
          type: "number",
        })
        .option("quantity", {
          type: "number",
        })
        .option("status", {
          type: "string",
          choices: ["placed", "approved", "delivered"],
        })
        .option("complete", {
          type: "boolean",
        })
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstoreStoreOrderPlaceCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstoreStoreOrderPlaceCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstoreStoreOrderPlaceCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstoreStoreOrderPlaceArgs>) => {
      if (argv.help) {
        actions.help(getPetstoreStoreOrderPlaceCmdHelpData());
        process.exit(0);
      }
      const cmdArgs: PetstoreStoreOrderPlaceArgs = {
        pathToOrderBody: argv.pathToOrderBody,
      };
      const cmdFlags: PetstoreStoreOrderPlaceFlags = {
        petId: argv.petId as number,
        quantity: argv.quantity as number,
        status: argv.status as PetstoreStoreOrderPlaceStatus,
        complete: argv.complete as boolean,
      };
      return actions.PetstoreStoreOrderPlace(cmdArgs, cmdFlags);
    },
  };
}

function getPetstoreStoreOrderPlaceCmdHelpData(): CommandPrintData {
  return {
    segment: "place",
    commandLine: "petstore store order place",
    summary: "Place an order for a pet",
    description: "Create a purchase order using a JSON payload or explicit flags.",
    visibleChildren: false,
    visibleArgs: true,
    visibleFlags: true,
    argsModifiers: [
      "<path-to-order-body>",
    ],
    flagsModifiers: [
      "[flags]",
    ],
    args: [
      { name: "path-to-order-body", summary: "The path to a JSON file containing the order payload", isRequired: false },
    ],
    flags: [
      { name: "pet-id", summary: "The ID of the pet to order", aliases: [] },
      { name: "quantity", summary: "How many pets to order", aliases: [] },
      { name: "status", summary: "The order status", aliases: [] },
      { name: "complete", summary: "Whether the order has been completed", aliases: [] },
    ],
  };
}
