// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
  PetstorePetDeleteFlags,
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstorePetDeleteArgs {
  "id": number;
  "apiKey"?: string;
  help: boolean;
}

export function newPetstorePetDeleteCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstorePetDeleteArgs> {
  return {
    command: "delete",
    describe: "Delete a pet",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstorePetDeleteArgs> => {
      return argv
        .option("id", {
          type: "number",
          demandOption: true,
        })
        .option("api-key", {
          type: "string",
        })
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstorePetDeleteCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstorePetDeleteCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstorePetDeleteCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstorePetDeleteArgs>) => {
      if (argv.help) {
        actions.help(getPetstorePetDeleteCmdHelpData());
        process.exit(0);
      }
      const cmdFlags: PetstorePetDeleteFlags = {
        id: argv.id as number,
        apiKey: argv.apiKey as string,
      };
      return actions.PetstorePetDelete(cmdFlags);
    },
  };
}

function getPetstorePetDeleteCmdHelpData(): CommandPrintData {
  return {
    segment: "delete",
    commandLine: "petstore pet delete",
    summary: "Delete a pet",
    visibleChildren: false,
    visibleArgs: false,
    visibleFlags: true,
    flagsModifiers: [
      "[flags]",
    ],
    flags: [
      { name: "id", summary: "The ID of the pet to delete", aliases: [] },
      { name: "api-key", summary: "API key header used to authorize the delete request", aliases: [] },
    ],
  };
}
