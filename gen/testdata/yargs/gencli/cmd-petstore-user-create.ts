// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
  PetstoreUserCreateArgs,
  PetstoreUserCreateFlags,
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstoreUserCreateArgs {
  "path-to-user-body"?: string;
  "username"?: string;
  "firstname"?: string;
  "lastname"?: string;
  "email"?: string;
  "password"?: string;
  "phone"?: string;
  "status"?: number;
  help: boolean;
}

export function newPetstoreUserCreateCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstoreUserCreateArgs> {
  return {
    command: "create <path-to-user-body>",
    describe: "Create a user",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstoreUserCreateArgs> => {
      return argv
        .positional("path-to-user-body", {
          describe: "",
          type: "string",
        })
        .option("username", {
          type: "string",
        })
        .option("firstName", {
          type: "string",
        })
        .option("lastName", {
          type: "string",
        })
        .option("email", {
          type: "string",
        })
        .option("password", {
          type: "string",
        })
        .option("phone", {
          type: "string",
        })
        .option("status", {
          type: "number",
          default: 0,
        })
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstoreUserCreateCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstoreUserCreateCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstoreUserCreateCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstoreUserCreateArgs>) => {
      if (argv.help) {
        actions.help(getPetstoreUserCreateCmdHelpData());
        process.exit(0);
      }
      const cmdArgs: PetstoreUserCreateArgs = {
        pathToUserBody: argv.pathToUserBody,
      };
      const cmdFlags: PetstoreUserCreateFlags = {
        username: argv.username as string,
        firstname: argv.firstname as string,
        lastname: argv.lastname as string,
        email: argv.email as string,
        password: argv.password as string,
        phone: argv.phone as string,
        status: argv.status as number,
      };
      return actions.PetstoreUserCreate(cmdArgs, cmdFlags);
    },
  };
}

function getPetstoreUserCreateCmdHelpData(): CommandPrintData {
  return {
    segment: "create",
    commandLine: "petstore user create",
    summary: "Create a user",
    description: "Create a new user using a JSON payload or explicit flags.",
    visibleChildren: false,
    visibleArgs: true,
    visibleFlags: true,
    argsModifiers: [
      "<path-to-user-body>",
    ],
    flagsModifiers: [
      "[flags]",
    ],
    args: [
      { name: "path-to-user-body", summary: "The path to a JSON file containing the user payload", isRequired: false },
    ],
    flags: [
      { name: "username", summary: "The user's username" },
      { name: "firstName", summary: "The user's first name" },
      { name: "lastName", summary: "The user's last name" },
      { name: "email", summary: "The user's email address" },
      { name: "password", summary: "The user's password" },
      { name: "phone", summary: "The user's phone number" },
      { name: "status", summary: "The user's status" },
    ],
  };
}
