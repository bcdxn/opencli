// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
  PetstoreStoreOrderGetFlags,
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstoreStoreOrderGetArgs {
  "id": number;
  help: boolean;
}

export function newPetstoreStoreOrderGetCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstoreStoreOrderGetArgs> {
  return {
    command: "get",
    describe: "Find purchase order by ID",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstoreStoreOrderGetArgs> => {
      return argv
        .option("id", {
          type: "number",
          demandOption: true,
        })
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstoreStoreOrderGetCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstoreStoreOrderGetCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstoreStoreOrderGetCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstoreStoreOrderGetArgs>) => {
      if (argv.help) {
        actions.help(getPetstoreStoreOrderGetCmdHelpData());
        process.exit(0);
      }
      const cmdFlags: PetstoreStoreOrderGetFlags = {
        id: argv.id as number,
      };
      return actions.PetstoreStoreOrderGet(cmdFlags);
    },
  };
}

function getPetstoreStoreOrderGetCmdHelpData(): CommandPrintData {
  return {
    segment: "get",
    commandLine: "petstore store order get",
    summary: "Find purchase order by ID",
    visibleChildren: false,
    visibleArgs: false,
    visibleFlags: true,
    flagsModifiers: [
      "[flags]",
    ],
    flags: [
      { name: "id", summary: "The ID of the order to retrieve", aliases: [] },
    ],
  };
}
