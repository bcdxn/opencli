// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { newPetstorePetAddCmd } from "./cmd-petstore-pet-add";
import { newPetstorePetUpdateCmd } from "./cmd-petstore-pet-update";
import { newPetstorePetFindByStatusCmd } from "./cmd-petstore-pet-find-by-status";
import { newPetstorePetFindByTagsCmd } from "./cmd-petstore-pet-find-by-tags";
import { newPetstorePetGetCmd } from "./cmd-petstore-pet-get";
import { newPetstorePetUpdateFormCmd } from "./cmd-petstore-pet-update-form";
import { newPetstorePetDeleteCmd } from "./cmd-petstore-pet-delete";
import { newPetstorePetUploadImageCmd } from "./cmd-petstore-pet-upload-image";
import { ActionsInterface } from "./actions";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
export function newPetstorePetCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}> {
  return {
    command: "pet",
    describe: "A collection of commands for managing pets",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<{}> => {
      return argv
        .command(newPetstorePetAddCmd(actions))
        .command(newPetstorePetUpdateCmd(actions))
        .command(newPetstorePetFindByStatusCmd(actions))
        .command(newPetstorePetFindByTagsCmd(actions))
        .command(newPetstorePetGetCmd(actions))
        .command(newPetstorePetUpdateFormCmd(actions))
        .command(newPetstorePetDeleteCmd(actions))
        .command(newPetstorePetUploadImageCmd(actions))
        .demandCommand(1)
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstorePetCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstorePetCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstorePetCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<{}>) => {
      if (argv.help) {
        actions.help(getPetstorePetCmdHelpData());
        process.exit(0);
      }
      return Promise.reject(
        createBadUserInputError("missing required sub command", () => {
          actions.usage(getPetstorePetCmdHelpData());
        }),
      );
    },
  };
}

function getPetstorePetCmdHelpData(): CommandPrintData {
  return {
    segment: "pet",
    commandLine: "petstore pet",
    summary: "A collection of commands for managing pets",
    visibleChildren: true,
    visibleArgs: false,
    visibleFlags: false,
    commandModifiers: [
      "{command}",
    ],
    argsModifiers: [
      "<arguments>",
    ],
    flagsModifiers: [
      "[flags]",
    ],
    commands: [
      { segment: "add", commandLine: "add", summary: "Add a new pet to the store" },
      { segment: "update", commandLine: "update", summary: "Update an existing pet" },
      { segment: "find-by-status", commandLine: "find-by-status", summary: "Find pets by status" },
      { segment: "find-by-tags", commandLine: "find-by-tags", summary: "Find pets by tags" },
      { segment: "get", commandLine: "get", summary: "Find pet by ID" },
      { segment: "update-form", commandLine: "update-form", summary: "Update a pet using form data" },
      { segment: "delete", commandLine: "delete", summary: "Delete a pet" },
      { segment: "upload-image", commandLine: "upload-image", summary: "Upload an image for a pet" },
    ],
  };
}
