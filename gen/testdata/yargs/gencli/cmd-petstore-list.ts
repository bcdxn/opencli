// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstoreListArgs {
  help: boolean;
}

export function newPetstoreListCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstoreListArgs> {
  return {
    command: "list",
    describe: "List all endpoints available",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstoreListArgs> => {
      return argv
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstoreListCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstoreListCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstoreListCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstoreListArgs>) => {
      if (argv.help) {
        actions.help(getPetstoreListCmdHelpData());
        process.exit(0);
      }
      return actions.PetstoreList();
    },
  };
}

function getPetstoreListCmdHelpData(): CommandPrintData {
  return {
    segment: "list",
    commandLine: "petstore list",
    summary: "List all endpoints available",
    visibleChildren: false,
    visibleArgs: false,
    visibleFlags: false,
  };
}
