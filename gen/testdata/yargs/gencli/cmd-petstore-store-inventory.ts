// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstoreStoreInventoryArgs {
  help: boolean;
}

export function newPetstoreStoreInventoryCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstoreStoreInventoryArgs> {
  return {
    command: "inventory",
    describe: "Returns pet inventories by status",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstoreStoreInventoryArgs> => {
      return argv
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstoreStoreInventoryCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstoreStoreInventoryCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstoreStoreInventoryCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstoreStoreInventoryArgs>) => {
      if (argv.help) {
        actions.help(getPetstoreStoreInventoryCmdHelpData());
        process.exit(0);
      }
      return actions.PetstoreStoreInventory();
    },
  };
}

function getPetstoreStoreInventoryCmdHelpData(): CommandPrintData {
  return {
    segment: "inventory",
    commandLine: "petstore store inventory",
    summary: "Returns pet inventories by status",
    visibleChildren: false,
    visibleArgs: false,
    visibleFlags: false,
  };
}
