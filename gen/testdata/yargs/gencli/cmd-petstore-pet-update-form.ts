// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
  PetstorePetUpdateFormFlags,
  PetstorePetUpdateFormStatus,
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstorePetUpdateFormArgs {
  "id": number;
  "name"?: string;
  "status"?: string;
  help: boolean;
}

export function newPetstorePetUpdateFormCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstorePetUpdateFormArgs> {
  return {
    command: "update-form",
    describe: "Update a pet using form data",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstorePetUpdateFormArgs> => {
      return argv
        .option("id", {
          type: "number",
          demandOption: true,
          default: 0,
        })
        .option("name", {
          type: "string",
        })
        .option("status", {
          type: "string",
          choices: ["available", "pending", "sold"],
        })
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstorePetUpdateFormCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstorePetUpdateFormCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstorePetUpdateFormCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstorePetUpdateFormArgs>) => {
      if (argv.help) {
        actions.help(getPetstorePetUpdateFormCmdHelpData());
        process.exit(0);
      }
      const cmdFlags: PetstorePetUpdateFormFlags = {
        id: argv.id as number,
        name: argv.name as string,
        status: argv.status as PetstorePetUpdateFormStatus,
      };
      return actions.PetstorePetUpdateForm(cmdFlags);
    },
  };
}

function getPetstorePetUpdateFormCmdHelpData(): CommandPrintData {
  return {
    segment: "update-form",
    commandLine: "petstore pet update-form",
    summary: "Update a pet using form data",
    visibleChildren: false,
    visibleArgs: false,
    visibleFlags: true,
    flagsModifiers: [
      "[flags]",
    ],
    flags: [
      { name: "id", summary: "The ID of the pet to update" },
      { name: "name", summary: "The new name for the pet" },
      { name: "status", summary: "The new status for the pet" },
    ],
  };
}
