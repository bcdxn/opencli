// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { newPetstoreStoreOrderPlaceCmd } from "./cmd-petstore-store-order-place";
import { newPetstoreStoreOrderGetCmd } from "./cmd-petstore-store-order-get";
import { newPetstoreStoreOrderDeleteCmd } from "./cmd-petstore-store-order-delete";
import { ActionsInterface } from "./actions";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
export function newPetstoreStoreOrderCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}> {
  return {
    command: "order",
    describe: "A collection of commands for purchase orders",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<{}> => {
      return argv
        .command(newPetstoreStoreOrderPlaceCmd(actions))
        .command(newPetstoreStoreOrderGetCmd(actions))
        .command(newPetstoreStoreOrderDeleteCmd(actions))
        .demandCommand(1)
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstoreStoreOrderCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstoreStoreOrderCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstoreStoreOrderCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<{}>) => {
      if (argv.help) {
        actions.help(getPetstoreStoreOrderCmdHelpData());
        process.exit(0);
      }
      return Promise.reject(
        createBadUserInputError("missing required sub command", () => {
          actions.usage(getPetstoreStoreOrderCmdHelpData());
        }),
      );
    },
  };
}

function getPetstoreStoreOrderCmdHelpData(): CommandPrintData {
  return {
    segment: "order",
    commandLine: "petstore store order",
    summary: "A collection of commands for purchase orders",
    visibleChildren: true,
    visibleArgs: false,
    visibleFlags: false,
    commandModifiers: [
      "{command}",
    ],
    argsModifiers: [
      "<arguments>",
    ],
    flagsModifiers: [
      "[flags]",
    ],
    commands: [
      { segment: "place", commandLine: "place", summary: "Place an order for a pet" },
      { segment: "get", commandLine: "get", summary: "Find purchase order by ID" },
      { segment: "delete", commandLine: "delete", summary: "Delete purchase order by ID" },
    ],
  };
}
