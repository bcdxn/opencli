// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstoreUserLogoutArgs {
  help: boolean;
}

export function newPetstoreUserLogoutCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstoreUserLogoutArgs> {
  return {
    command: "logout",
    describe: "Log out the current user",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstoreUserLogoutArgs> => {
      return argv
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstoreUserLogoutCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstoreUserLogoutCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstoreUserLogoutCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstoreUserLogoutArgs>) => {
      if (argv.help) {
        actions.help(getPetstoreUserLogoutCmdHelpData());
        process.exit(0);
      }
      return actions.PetstoreUserLogout();
    },
  };
}

function getPetstoreUserLogoutCmdHelpData(): CommandPrintData {
  return {
    segment: "logout",
    commandLine: "petstore user logout",
    summary: "Log out the current user",
    visibleChildren: false,
    visibleArgs: false,
    visibleFlags: false,
  };
}
