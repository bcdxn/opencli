// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { newPetstoreUserCreateCmd } from "./cmd-petstore-user-create";
import { newPetstoreUserCreateWithListCmd } from "./cmd-petstore-user-create-with-list";
import { newPetstoreUserLoginCmd } from "./cmd-petstore-user-login";
import { newPetstoreUserLogoutCmd } from "./cmd-petstore-user-logout";
import { newPetstoreUserGetCmd } from "./cmd-petstore-user-get";
import { newPetstoreUserUpdateCmd } from "./cmd-petstore-user-update";
import { newPetstoreUserDeleteCmd } from "./cmd-petstore-user-delete";
import { ActionsInterface } from "./actions";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
export function newPetstoreUserCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}> {
  return {
    command: "user",
    describe: "",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<{}> => {
      return argv
        .command(newPetstoreUserCreateCmd(actions))
        .command(newPetstoreUserCreateWithListCmd(actions))
        .command(newPetstoreUserLoginCmd(actions))
        .command(newPetstoreUserLogoutCmd(actions))
        .command(newPetstoreUserGetCmd(actions))
        .command(newPetstoreUserUpdateCmd(actions))
        .command(newPetstoreUserDeleteCmd(actions))
        .demandCommand(1)
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstoreUserCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstoreUserCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstoreUserCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<{}>) => {
      if (argv.help) {
        actions.help(getPetstoreUserCmdHelpData());
        process.exit(0);
      }
      return Promise.reject(
        createBadUserInputError("missing required sub command", () => {
          actions.usage(getPetstoreUserCmdHelpData());
        }),
      );
    },
  };
}

function getPetstoreUserCmdHelpData(): CommandPrintData {
  return {
    segment: "user",
    commandLine: "petstore user",
    summary: "",
    visibleChildren: true,
    visibleArgs: false,
    visibleFlags: false,
    commandModifiers: [
      "{command}",
    ],
    argsModifiers: [
      "<arguments>",
    ],
    flagsModifiers: [
      "[flags]",
    ],
    commands: [
      { segment: "create", commandLine: "create", summary: "Create a user" },
      { segment: "create-with-list", commandLine: "create-with-list", summary: "Create multiple users with a list" },
      { segment: "login", commandLine: "login", summary: "Log in a user" },
      { segment: "logout", commandLine: "logout", summary: "Log out the current user" },
      { segment: "get", commandLine: "get", summary: "Get user by username" },
      { segment: "update", commandLine: "update", summary: "Update a user" },
      { segment: "delete", commandLine: "delete", summary: "Delete a user" },
    ],
  };
}
