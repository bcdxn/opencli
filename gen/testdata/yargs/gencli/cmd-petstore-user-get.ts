// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
  PetstoreUserGetFlags,
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstoreUserGetArgs {
  "username": string;
  help: boolean;
}

export function newPetstoreUserGetCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstoreUserGetArgs> {
  return {
    command: "get",
    describe: "Get user by username",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstoreUserGetArgs> => {
      return argv
        .option("username", {
          type: "string",
          demandOption: true,
        })
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstoreUserGetCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstoreUserGetCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstoreUserGetCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstoreUserGetArgs>) => {
      if (argv.help) {
        actions.help(getPetstoreUserGetCmdHelpData());
        process.exit(0);
      }
      const cmdFlags: PetstoreUserGetFlags = {
        username: argv.username as string,
      };
      return actions.PetstoreUserGet(cmdFlags);
    },
  };
}

function getPetstoreUserGetCmdHelpData(): CommandPrintData {
  return {
    segment: "get",
    commandLine: "petstore user get",
    summary: "Get user by username",
    visibleChildren: false,
    visibleArgs: false,
    visibleFlags: true,
    flagsModifiers: [
      "[flags]",
    ],
    flags: [
      { name: "username", summary: "The username to look up" },
    ],
  };
}
