// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
  PetstorePetGetFlags,
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstorePetGetArgs {
  "id": number;
  help: boolean;
}

export function newPetstorePetGetCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstorePetGetArgs> {
  return {
    command: "get",
    describe: "Find pet by ID",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstorePetGetArgs> => {
      return argv
        .option("id", {
          type: "number",
          demandOption: true,
          default: 0,
        })
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstorePetGetCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstorePetGetCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstorePetGetCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstorePetGetArgs>) => {
      if (argv.help) {
        actions.help(getPetstorePetGetCmdHelpData());
        process.exit(0);
      }
      const cmdFlags: PetstorePetGetFlags = {
        id: argv.id as number,
      };
      return actions.PetstorePetGet(cmdFlags);
    },
  };
}

function getPetstorePetGetCmdHelpData(): CommandPrintData {
  return {
    segment: "get",
    commandLine: "petstore pet get",
    summary: "Find pet by ID",
    visibleChildren: false,
    visibleArgs: false,
    visibleFlags: true,
    flagsModifiers: [
      "[flags]",
    ],
    flags: [
      { name: "id", summary: "The ID of the pet to retrieve" },
    ],
  };
}
