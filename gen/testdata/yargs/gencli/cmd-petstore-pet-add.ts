// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
  PetstorePetAddArgs,
  PetstorePetAddFlags,
  PetstorePetAddStatus,
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstorePetAddArgs {
  "path-to-req-body"?: string;
  "name"?: string;
  "photoUrls"?: string[] | undefined;
  "status"?: string;
  "tag"?: string[] | undefined;
  help: boolean;
}

export function newPetstorePetAddCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstorePetAddArgs> {
  return {
    command: "add <path-to-req-body>",
    describe: "Add a new pet to the store",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstorePetAddArgs> => {
      return argv
        .positional("path-to-req-body", {
          describe: "",
          type: "string",
        })
        .option("name", {
          aliases: ["n"],
          type: "string",
        })
        .option("photo-urls", {
          aliases: ["p"],
          type: "array",
          string: true,
        })
        .option("status", {
          type: "string",
          choices: ["available", "pending", "sold"],
        })
        .option("tag", {
          type: "array",
          string: true,
        })
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstorePetAddCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstorePetAddCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstorePetAddCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstorePetAddArgs>) => {
      if (argv.help) {
        actions.help(getPetstorePetAddCmdHelpData());
        process.exit(0);
      }
      const cmdArgs: PetstorePetAddArgs = {
        pathToReqBody: argv.pathToReqBody,
      };
      const cmdFlags: PetstorePetAddFlags = {
        name: argv.name as string,
        photoUrls: argv.photoUrls as string[],
        status: argv.status as PetstorePetAddStatus,
        tag: argv.tag as string[],
      };
      return actions.PetstorePetAdd(cmdArgs, cmdFlags);
    },
  };
}

function getPetstorePetAddCmdHelpData(): CommandPrintData {
  return {
    segment: "add",
    commandLine: "petstore pet add",
    summary: "Add a new pet to the store",
    description: "Create a new pet in the store using a JSON payload or explicit flags.",
    visibleChildren: false,
    visibleArgs: true,
    visibleFlags: true,
    argsModifiers: [
      "<arguments>",
    ],
    flagsModifiers: [
      "[flags]",
    ],
    args: [
      { name: "path-to-req-body", summary: "The path to a JSON file containing the new pet payload", isRequired: false },
    ],
    flags: [
      { name: "name", summary: "The name of the pet", aliases: ["n"] },
      { name: "photo-urls", summary: "A list of photo URLs to display for the pet", aliases: ["p"] },
      { name: "status", summary: "The pet status in the store", aliases: [] },
      { name: "tag", summary: "Tag to assign to the pet for grouping/sorting", aliases: [] },
    ],
  };
}
