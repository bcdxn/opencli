// Code generated by github.com/bcdxn/opencli@(devel) DO NOT EDIT.
import yargs from "yargs";
import { ActionsInterface } from "./actions";
import {
  PetstorePetUpdateArgs,
  PetstorePetUpdateFlags,
  PetstorePetUpdateStatus,
} from "./params";
import { CommandPrintData } from "./types";
import { CliError, ExitCode, createBadUserInputError } from "./errors";
// Local argv shape for this command's builder.
interface petstorePetUpdateArgs {
  "path-to-req-body"?: string;
  "name"?: string;
  "status"?: string;
  "photoUrls"?: string[] | undefined;
  "tags"?: string[] | undefined;
  help: boolean;
}

export function newPetstorePetUpdateCmd(
  actions: ActionsInterface,
): yargs.CommandModule<{}, petstorePetUpdateArgs> {
  return {
    command: "update <path-to-req-body>",
    describe: "Update an existing pet",
    builder: (argv: yargs.Argv<{}>): yargs.Argv<petstorePetUpdateArgs> => {
      return argv
        .positional("path-to-req-body", {
          describe: "",
          type: "string",
        })
        .option("name", {
          aliases: ["n"],
          type: "string",
        })
        .option("status", {
          type: "string",
          choices: ["available", "pending", "sold"],
        })
        .option("photo-urls", {
          aliases: ["p"],
          type: "array",
          string: true,
        })
        .option("tags", {
          type: "array",
          string: true,
        })
        .help(false)
        .option("help", {
          alias: "h",
          type: "boolean",
          default: false,
        })
        .fail(async (msg: string, err: Error) => {
          if (process.argv.includes("--help") || process.argv.includes("-h")) {
            actions.help(getPetstorePetUpdateCmdHelpData());
            process.exit(0);
          }

          if (err instanceof CliError) {
            console.log(err.message);
            actions.usage(getPetstorePetUpdateCmdHelpData());
            process.exit(err.code);
          }

          console.log(msg);
          actions.usage(getPetstorePetUpdateCmdHelpData());
          if (
            msg.includes("Missing required arguments") ||
            msg.includes("Not enough non-option arguments")
          ) {
            process.exit(ExitCode.BadUserInputErr);
          }
          process.exit(ExitCode.InternalErr);
        });
    },
    handler: async (argv: yargs.ArgumentsCamelCase<petstorePetUpdateArgs>) => {
      if (argv.help) {
        actions.help(getPetstorePetUpdateCmdHelpData());
        process.exit(0);
      }
      const cmdArgs: PetstorePetUpdateArgs = {
        pathToReqBody: argv.pathToReqBody,
      };
      const cmdFlags: PetstorePetUpdateFlags = {
        name: argv.name as string,
        status: argv.status as PetstorePetUpdateStatus,
        photoUrls: argv.photoUrls as string[],
        tags: argv.tags as string[],
      };
      return actions.PetstorePetUpdate(cmdArgs, cmdFlags);
    },
  };
}

function getPetstorePetUpdateCmdHelpData(): CommandPrintData {
  return {
    segment: "update",
    commandLine: "petstore pet update",
    summary: "Update an existing pet",
    description: "Update a pet using a JSON payload or explicit flags.",
    visibleChildren: false,
    visibleArgs: true,
    visibleFlags: true,
    argsModifiers: [
      "<path-to-req-body>",
    ],
    flagsModifiers: [
      "[flags]",
    ],
    args: [
      { name: "path-to-req-body", summary: "The path to a JSON file containing the updated pet payload", isRequired: false },
    ],
    flags: [
      { name: "name", summary: "The updated name of the pet" },
      { name: "status", summary: "The updated pet status" },
      { name: "photo-urls", summary: "A list of photo URLs to display for the pet" },
      { name: "tags", summary: "Tags to assign to the pet" },
    ],
  };
}
